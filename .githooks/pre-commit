#!/bin/sh
set -eu

if command -v python >/dev/null 2>&1; then
  PYTHON_BIN=python
elif command -v python3 >/dev/null 2>&1; then
  PYTHON_BIN=python3
else
  echo "[branch-guard] Python is required to run branch path checks." >&2
  exit 1
fi

"$PYTHON_BIN" scripts/branch_path_guard.py --staged
